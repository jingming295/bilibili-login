interface Config
{
  SESSDATA: string;
  csrf: string;
  refresh_token: string;
  RefreshTimeHours:number
}

interface BilibiliAccountData
{
  SESSDATA: string;
  csrf: string;
  refresh_token: string;
}